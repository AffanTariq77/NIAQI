# 🏗️ Architecture Overview - Kajabi Integration

## Before vs After

### ❌ BEFORE: Static Course List

```
┌─────────────────────────────────┐
│   Courses Screen                │
│                                 │
│  ┌──────────────────────────┐  │
│  │ Course Card 1            │  │
│  │ - Static data            │  │
│  │ - Mock thumbnail         │  │
│  └──────────────────────────┘  │
│                                 │
│  ┌──────────────────────────┐  │
│  │ Course Card 2            │  │
│  └──────────────────────────┘  │
│                                 │
│  ┌──────────────────────────┐  │
│  │ Course Card 3            │  │
│  └──────────────────────────┘  │
│                                 │
└─────────────────────────────────┘
```

### ✅ AFTER: Kajabi Storefront Embed

```
┌─────────────────────────────────┐
│   Courses Screen                │
│                                 │
│  ╔═════════════════════════╗  │
│  ║                         ║  │
│  ║   Kajabi WebView        ║  │
│  ║   (Fullscreen)          ║  │
│  ║                         ║  │
│  ║   • Browse courses      ║  │
│  ║   • Login/Register      ║  │
│  ║   • Add to cart         ║  │
│  ║   • Checkout            ║  │
│  ║   • View purchases      ║  │
│  ║                         ║  │
│  ║   [All Kajabi content]  ║  │
│  ║                         ║  │
│  ╚═════════════════════════╝  │
│                                 │
└─────────────────────────────────┘
```

## Component Architecture

```
┌────────────────────────────────────────────────┐
│                                                │
│  CoursesScreen Component                      │
│                                                │
│  ┌──────────────────────────────────────┐    │
│  │  SafeAreaView                        │    │
│  │                                      │    │
│  │  ┌────────────────────────────────┐ │    │
│  │  │  WebView Container             │ │    │
│  │  │                                │ │    │
│  │  │  ┌──────────────────────────┐ │ │    │
│  │  │  │  WebView Component       │ │ │    │
│  │  │  │                          │ │ │    │
│  │  │  │  source: Kajabi URL      │ │ │    │
│  │  │  │  JavaScript: Enabled     │ │ │    │
│  │  │  │  Cookies: Enabled        │ │ │    │
│  │  │  │  Scrollbars: Hidden      │ │ │    │
│  │  │  │                          │ │ │    │
│  │  │  └──────────────────────────┘ │ │    │
│  │  │                                │ │    │
│  │  │  ┌──────────────────────────┐ │ │    │
│  │  │  │  Loading Indicator       │ │ │    │
│  │  │  │  (when loading)          │ │ │    │
│  │  │  └──────────────────────────┘ │ │    │
│  │  │                                │ │    │
│  │  └────────────────────────────────┘ │    │
│  │                                      │    │
│  └──────────────────────────────────────┘    │
│                                                │
└────────────────────────────────────────────────┘
```

## Data Flow

```
┌──────────────┐
│   .env File  │
│              │
│  Kajabi URL  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  config.ts   │
│              │
│ KAJABI_CONFIG│
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│  courses.tsx     │
│                  │
│  Import config   │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  WebView         │
│                  │
│  Load Kajabi     │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  User's Device   │
│                  │
│  Display content │
└──────────────────┘
```

## State Management

```
┌─────────────────────────────────────────┐
│  Component State                        │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  isLoading: boolean               │ │
│  │  - true: Show loading indicator   │ │
│  │  - false: Show WebView content    │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  webViewRef: RefObject<WebView>   │ │
│  │  - Reference to WebView           │ │
│  │  - For future navigation controls │ │
│  └───────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

## Event Flow

```
User Action          →  Component Event        →  Result
─────────────────────────────────────────────────────────
App opens           →  WebView loads          →  Show loading
                    →                         →
Kajabi loads        →  onLoad() fires         →  Hide loading
                    →                         →  Show content
                    →                         →
User scrolls        →  Native scroll          →  Smooth scroll
                    →  (no page scroll)       →
                    →                         →
User clicks course  →  Kajabi handles         →  Course details
                    →                         →
User logs in        →  Kajabi handles         →  Authentication
                    →  Cookies stored         →  Stay logged in
                    →                         →
User checks out     →  Kajabi handles         →  Payment process
                    →                         →
External link       →  JavaScript injection   →  Open in browser
                    →  (optional)             →  (if configured)
```

## Security & Privacy

```
┌─────────────────────────────────────────┐
│  Security Features                      │
│                                         │
│  ✅ HTTPS only (Kajabi enforced)        │
│  ✅ Third-party cookies enabled         │
│  ✅ Shared cookies for auth             │
│  ✅ DOM storage for session             │
│  ✅ Mixed content compatibility         │
│  ✅ JavaScript sandboxed                │
│                                         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Data Handling                          │
│                                         │
│  • Login: Kajabi manages                │
│  • Payments: Kajabi manages             │
│  • User data: Kajabi stores             │
│  • App only displays content            │
│                                         │
└─────────────────────────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────┐
│  Optimizations                          │
│                                         │
│  1. Single WebView instance             │
│     - No multiple WebViews              │
│     - Efficient memory usage            │
│                                         │
│  2. Loading states                      │
│     - Show indicator immediately        │
│     - Hide when content ready           │
│                                         │
│  3. Scroll performance                  │
│     - Native scrolling                  │
│     - Hardware acceleration             │
│     - No nested scrolls                 │
│                                         │
│  4. Cookie persistence                  │
│     - Stay logged in                    │
│     - No repeated logins                │
│                                         │
└─────────────────────────────────────────┘
```

## Platform Compatibility

```
┌──────────┬──────────┬─────────────────────────┐
│ Platform │ Status   │ Notes                   │
├──────────┼──────────┼─────────────────────────┤
│ iOS      │ ✅ Full  │ Native WebKit           │
│ Android  │ ✅ Full  │ Chrome WebView          │
│ Web      │ ⚠️ Basic│ Use iframe (limited)    │
└──────────┴──────────┴─────────────────────────┘
```

## File Structure

```
/NIAQI/
├── .env                              ← Kajabi URL config
├── app/
│   └── (tabs)/
│       ├── courses.tsx               ← Main implementation
│       └── COURSES_README.md         ← Documentation
├── lib/
│   └── config.ts                     ← Kajabi config
├── IMPLEMENTATION_SUMMARY.md         ← Full details
└── QUICK_START.md                    ← Quick reference
```

## Dependencies

```
Package                  Version    Purpose
─────────────────────────────────────────────────
react-native-webview     Latest     WebView component
react-native            0.81.5     Framework
expo                    ~54.0.19   Build system
```

## Future Enhancements

```
┌─────────────────────────────────────────┐
│  Potential Features                     │
│                                         │
│  🔄 Pull to refresh                     │
│  ← → Back/forward navigation            │
│  🔗 Deep linking to courses             │
│  💾 Offline caching                     │
│  📊 Analytics tracking                  │
│  📱 Native sharing                      │
│  🔔 Push notifications                  │
│  🌐 Multi-language support              │
│                                         │
└─────────────────────────────────────────┘
```

---

**This architecture provides**:

- ✅ Separation of concerns
- ✅ Easy configuration
- ✅ Maintainable code
- ✅ Scalable solution
- ✅ Platform compatibility
- ✅ Security best practices
